<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="cron_patient_automation" model="ir.cron">
        <field name="name">Patient Age Automation</field>
        <field name="active" eval="True"/>
        <field name="model_id" ref="model_hospital_patient"/>
        <field name="state">code</field>
        <field name="code">model._cron_update_patients()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(datetime.now() + timedelta(minutes=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>

</odoo>